package main

import (
	"time"
)

var (
	STARTTIME2PM = time.Date(0, 0, 0, 14, 0, 0, 0, time.UTC)
	ENDTIME4PM   = time.Date(0, 0, 0, 16, 0, 0, 0, time.UTC)
)

const (
	YYYY_MM_DD = "2006-01-02"
	HHMM       = "15:04"
)
